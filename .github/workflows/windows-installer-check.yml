name: Windows runner probe
on: [push]
permissions: {}
jobs:
  probe:
    runs-on: windows-latest
    timeout-minutes: 3
    steps:
      - shell: powershell
        run: Write-Host $PSVersionTable.PSVersion
